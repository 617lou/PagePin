# 更新日志

**[Changelog (EN)](CHANGELOG.md)**

记录 PagePin 各版本的功能与修复。

## 0.1.4 — 2026-08-06

### 改进

- **批注列表的层级与拖动**：
  - 添加批注状态：批注列表提升至最上层，不再被序号图标盖住；序号图标仍保持在备注对话框遮罩之上，对话框打开时点击其他序号一步切换的行为不变。
  - 查看批注状态（导出文件）：批注列表的层级提升至所有序号图标之上；点击序号弹出的批注弹窗提升至最上层，高于批注列表。
  - 两种状态下，批注列表都可以按住头部（标题栏）拖动到任意位置，避免遮挡页面内容；头部上的关闭按钮仍可正常点击。
- **批注列表增加最大高度（窗口高度的 80%）**：批注较多时，列表面板不再一路向高处撑开；超过最大高度后在列表内部出现滚动条，标题和关闭按钮保持可见。扩展与导出查看器同步生效。

### 修复

- **导出文件上二次批注刷新后丢失**：此前在导出的 `.annotated.html` 上用扩展继续添加的批注只存在于页面内存中，刷新后丢失且点击扩展图标也无法恢复。现在二次批注同样按页面网址自动保存到本地存储，重新打开导出文件时优先恢复本地记录（比文件内嵌数据更新）；再次导出成功后照常清除。
- **编辑批注时序号图标不再隐藏**：此前打开备注对话框编辑某条批注时，该条批注的序号图标会从页面上消失，无法对照确认批注对应的是哪个元素。现在对话框打开期间，正在编辑的批注序号图标保持显示。

## 0.1.3 — 2026-08-05

### 修复

- **备注对话框点遮罩会误关**：输入批注时点击对话框外的空白区域会直接关闭对话框、丢失已输入的内容。现在点击遮罩不再关闭对话框，只能通过 **取消** 或 Esc 关闭。
- **批注在页面跳转 / 刷新后丢失**：此前批注只保存在当前页面的内存与 DOM 中，误点链接跳到别的页面、刷新页面或标签页被浏览器回收后全部丢失。现在批注按页面网址（不含 `#` 锚点）自动保存到浏览器本地存储，刷新或重新打开页面时批注自动恢复，在导出成功前一直保留。自动恢复覆盖本地 `file://` 页面与 `localhost` / `127.0.0.1` 开发服务器页面；其他 http(s) 网页因未申请全站权限，仍通过点击扩展图标恢复。

### 变更

- **导出成功后自动清除本页批注**：批注已完整写入导出文件，导出成功后当前页面上的批注及本地保存的记录一并清除，下一轮批注从空白开始。

### 改进

- **查看批注弹窗增加最大高度**：批注内容较长时，弹窗不再无限撑高甚至超出窗口；内容超过最大高度后在弹窗内部出现滚动条，标题和关闭按钮保持可见。

## 0.1.2 — 2026-08-04

### 修复

- **批注列表"定位"有时需要点两次**：此前页面 DOM 变动后（防抖 200ms）列表会被整体销毁重建，若重建恰好落在点击的按下与抬起之间，这次点击会被吞掉；且按钮闭包引用的是渲染时的目标元素，页面把被批注元素销毁重建后，第一次点击会静默失效。现改为列表条目按批注 id 复用（刷新时只更新文本、状态、禁用态），"定位""编辑"在点击时实时解析目标元素。扩展与导出查看器同步修复。
- **保存批注编辑后列表文字未及时更新**：此前要等页面下一次 DOM 变动才刷新，现在保存后立即刷新列表。

## 0.1.1 — 2026-08-04

### 修复

- **序号图标有时需要点两次**（添加批注与导出查看两种模式）：此前每次重渲染（滚动/缩放、页面 DOM 变动后 200ms）都会销毁并重建所有序号按钮，重建若落在点击的按下与抬起之间，点击会被浏览器重定向到没有监听器的容器上而丢失。现改为序号按钮按批注 id 复用（只更新位置与序号），点击时实时解析目标元素。

### 改进

- 序号图标层级提升至批注对话框遮罩之上：对话框打开时点击其他序号可一步切换，不再需要先点一下关掉对话框。

### 新增

- 扩展图标（蓝色图钉，16/32/48/128 PNG），用于工具栏与扩展管理页；由零依赖脚本 `tools/make-icons.js` 生成，改配色/造型后可直接重跑生成。

## 0.1.0

首个版本。

- 元素拾取（DevTools 式悬停高亮、点击批注）
- 序号图标（删除后自动重新编号）
- 批注列表（定位、编辑、删除）
- 导出为自带批注的单文件 HTML，无需扩展即可查看
- 批注以数据节点形式保存在当前页面内
- 序号图标显示开关
