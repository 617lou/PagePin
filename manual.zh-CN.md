# PagePin 操作手册

---

## 一、添加批注（编辑者）

> 适用场景：在浏览器中打开 HTML 页面，使用 PagePin 扩展为页面元素添加批注，最终导出带批注的 HTML 文件。

![添加批注](添加批注.png)

### 1. 准备工作

- 已安装 PagePin 扩展（Chrome 或 Edge）。
- 在浏览器中打开目标 HTML 页面。
- 如果是本地 `file://` 文件，需先在扩展详情页开启 **允许访问文件网址**。

### 2. 打开工具菜单

点击浏览器工具栏中的 PagePin 扩展图标，页面右下角弹出悬浮菜单。

> 菜单可拖动：按住标题栏（图钉图标及"PagePin / N 条批注"区域）拖到任意位置，避免遮挡页面内容。

菜单包含四个按钮：

| 按钮 | 功能 |
|------|------|
| 开始批注 | 进入元素选取模式 |
| 隐藏批注 / 显示批注 | 切换所有序号图标的显示 |
| 批注列表 | 打开批注列表面板 |
| 导出 HTML | 导出带批注的独立 HTML 文件 |

### 3. 添加一条批注

1. 点击菜单中的 **开始批注**。
2. 页面进入选取模式，鼠标移动时元素会被蓝色边框高亮。
3. 点击目标元素，弹出备注对话框。
4. 输入批注内容，点击 **保存**（或按 Ctrl+Enter）。
5. 元素右上角出现带序号的圆形图标（如 ①），表示批注已添加。

> 按 Esc 可取消选取模式。

> 点击对话框外的空白区域不会关闭对话框，不用担心误触丢失已输入的内容；需要关闭时点击 **取消** 或按 Esc。

### 4. 编辑 / 删除批注

- **点击序号图标**：弹出备注对话框，可修改内容后保存，或点击 **删除** 移除该批注。
- 删除后，后续批注的序号自动递减，不会出现断号。

### 5. 使用批注列表

点击菜单中的 **批注列表**，页面右上角打开列表面板，按序号展示所有批注。

> 列表面板显示在所有序号图标之上，可按住头部（标题栏）拖动到任意位置，避免遮挡页面内容；批注较多时，在列表内部滚动查看。

每条批注显示：
- 序号 + 目标元素描述（如 `#1 button.submit`）
- 批注内容
- 三个操作按钮

| 按钮 | 功能 |
|------|------|
| 定位 | 页面滚动到目标元素并高亮闪烁 |
| 编辑 | 打开备注对话框修改内容 |
| 删除 | 移除该批注 |

目标元素状态说明：
- 正常显示元素标签 → 元素可见，所有操作可用
- 显示 **（已隐藏）** → 元素在页面中但当前不可见（如在已关闭的弹窗内），定位和编辑仍可用
- 显示 **目标元素已丢失** → 元素已从页面移除，定位和编辑不可用，仅可删除

### 6. 处理页面弹窗中的元素

如果批注目标在页面自身的弹窗（如模态框、下拉面板）内：

- 弹窗关闭后，该元素的序号图标自动隐藏。
- 弹窗重新打开后，序号图标自动恢复，批注不会丢失。
- 批注列表中该条目会显示 **（已隐藏）** 状态，弹窗打开后恢复正常。

### 7. 导出 HTML

1. 点击菜单中的 **导出 HTML**。
2. 浏览器弹出下载保存窗口，文件名默认为 `原文件名.annotated.html`。
3. 保存后得到一份独立的 HTML 文件，内嵌全部批注数据和查看脚本。

> 导出的文件无需安装扩展即可查看批注。也可再次用扩展打开继续编辑；继续添加 / 修改的批注同样按网址自动保存到本地，刷新或重新打开文件不会丢失，直到再次导出成功后清除。

> 导出成功后，当前页面上的批注会自动清除（批注已完整写入导出文件），详见下一节。

### 8. 批注的自动保存与清除

- 批注按页面网址（不含 `#` 锚点）自动保存到浏览器本地存储，无需手动操作。
- 批注过程中不小心点到别的页面、刷新了页面甚至关闭了标签页，都不用担心：重新打开该页面，批注会自动恢复，无需其他操作。
- 批注会一直保留，直到 **导出 HTML** 成功：导出成功后，页面上的批注和本地保存的记录一并清除，下一轮批注从空白开始。

> 自动恢复覆盖本地 `file://` 页面（需已开启"允许访问文件网址"，这本来就是使用前提）与 `localhost` / `127.0.0.1` 开发服务器页面。在其他 http(s) 网页上，扩展未申请全站常驻权限，刷新后点击扩展图标即可恢复批注。

---

## 二、查看批注（阅读者）

> 适用场景：收到一份导出的 `.annotated.html` 文件，在浏览器中打开查看批注内容。无需安装任何扩展。

![在导出页面中查看批注](导出的页面查看批注.png)

### 1. 打开文件

直接用浏览器打开 `.annotated.html` 文件即可。页面加载后，右下角自动出现查看器工具栏。

> 工具栏可拖动：按住左侧图钉图标拖到任意位置。

工具栏包含：

| 元素 | 功能 |
|------|------|
| N 条批注 | 显示批注总数 |
| 隐藏 / 显示 | 切换所有序号图标的显示 |
| 批注列表 | 打开批注列表面板 |

### 2. 通过序号图标查看

- 页面中已批注的元素右上角显示带序号的圆形图标（如 ①②③）。
- 点击图标，弹出小面板显示该条批注的序号和内容。
- 内容较长时，面板内部出现滚动条，可在面板内滚动查看完整内容。
- 点击面板上的 **x** 关闭。

### 3. 通过批注列表查看

点击工具栏中的 **批注列表**，页面右上角打开列表面板，按序号展示所有批注。

> 列表面板显示在所有序号图标之上，可按住头部（标题栏）拖动到任意位置，批注较多时在列表内部滚动查看；点击序号弹出的批注弹窗显示在列表面板之上。

每条批注显示：
- 序号 + 目标元素描述
- 批注内容
- **定位** 按钮：点击后页面滚动到目标元素，并以蓝色边框高亮闪烁约 1 秒

目标元素状态说明：
- 正常显示 → 定位可用
- 显示 **（已隐藏）** → 元素在页面中但当前不可见（如在未展开的折叠区域内）
- 显示 **目标元素已丢失** → 元素不存在于当前页面，定位不可用

### 4. 处理页面弹窗中的批注

如果某条批注的目标元素在页面弹窗内：

- 弹窗未打开时，该元素的序号图标不显示，列表中显示 **（已隐藏）**。
- 打开弹窗后，序号图标自动出现，列表状态自动恢复正常。
- 此时可正常点击查看或定位。

---

## 常见问题

**Q：点击扩展图标没有反应？**
A：Chrome/Edge 内部页面（如 `chrome://`、扩展商店页）不允许注入脚本。请在普通 HTML 页面上使用。本地文件需在扩展详情页开启"允许访问文件网址"。

**Q：导出后打开文件看不到批注？**
A：确认打开的是 `.annotated.html` 后缀的导出文件，而非原始 HTML。导出文件内嵌了批注数据，原始文件没有。

**Q：批注时不小心点到别的页面，批注会丢吗？**
A：不会。批注已按页面网址自动保存到浏览器本地：本地 `file://` 页面和 `localhost` / `127.0.0.1` 开发服务器页面刷新或重新打开后自动恢复；其他 http(s) 网页刷新后点击扩展图标即可恢复。只有导出 HTML 成功后才会清除。

**Q：批注列表显示"目标元素已丢失"？**
A：该批注对应的 HTML 元素已不存在于页面中。如果页面有动态弹窗，尝试打开对应弹窗后刷新列表。如果元素确实已被移除，该批注仅可删除。


